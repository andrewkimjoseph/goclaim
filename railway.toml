[build]
builder = "NIXPACKS"
buildCommand = "node_modules/.bin/prisma generate"

[deploy]
startCommand = "node_modules/.bin/tsx worker/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
